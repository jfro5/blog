<%= form_for ([@article, @article.comments.build]),:html => {:class => "sky-form"} do |f| %>
      <%= f.label :commenter %><br>
      <%= f.text_field :commenter %>
    </p>
    <p>
      <%= f.label :body %><br>
      <%= f.text_area :body %>
    </p>
    <p>
      <button type="submit" class="btn primary">Submit</button>
    </p>
<% end %>